# Phantom-Text
Unicode steganography tool that hides secret messages within visible text using invisible zero-width characters. It allows users to encode and decode hidden data while keeping the visible cover text unchanged.
